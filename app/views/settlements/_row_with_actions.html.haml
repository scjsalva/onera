.relative.flex.items-center
  .min-w-0.flex-1
    = render "shared/settlement_row", settlement: settlement
  - unless settlement.voided?
    = link_to "Edit", edit_group_settlement_path(group, settlement),
              class: "press mr-4 shrink-0 text-xs font-semibold text-brand-600"
