default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: onera_development

test:
  <<: *default
  database: onera_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
